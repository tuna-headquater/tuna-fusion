apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: archive-pvc
  namespace: tuna-fusion
spec:
  storageClassName: standard
  accessModes:
    - ReadWriteMany
  volumeMode: Filesystem
  resources:
    requests:
      storage: 1Gi
