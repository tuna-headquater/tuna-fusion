apiVersion: v1
kind: Service
metadata:
  name: {{ include "tuna-fusion-operator.fullname" . }}
  labels:
    {{- include "tuna-fusion-operator.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: 80
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "tuna-fusion-operator.selectorLabels" . | nindent 4 }}
