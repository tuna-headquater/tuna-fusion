apiVersion: v1
kind: Service
metadata:
  name: {{ include "tuna-fusion.fullname" . }}
  labels:
    {{- include "tuna-fusion.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "tuna-fusion.selectorLabels" . | nindent 4 }}
