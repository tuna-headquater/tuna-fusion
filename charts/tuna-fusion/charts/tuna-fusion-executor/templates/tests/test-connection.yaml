apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "tuna-fusion-executor.fullname" . }}-test-connection"
  labels:
    {{- include "tuna-fusion-executor.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "shared.executorBaseUrl" . }}/actuator/health/readiness']
  restartPolicy: Never
